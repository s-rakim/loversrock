# Point this at your PC's local IP (not localhost — the emulator/phone can't reach that),
# e.g. http://192.168.1.50:4000. Use an ngrok URL instead if testing off your home WiFi.
EXPO_PUBLIC_API_URL=http://192.168.1.50:4000
